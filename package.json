{
    "name": "moncvpro",
    "version": "1.0.0",
    "description": "CV Master AI - Professional CV Builder with AI",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
        "build": "npm run build --prefix backend && npm run build --prefix frontend",
        "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
        "prisma:generate": "npm run prisma:generate --prefix backend",
        "prisma:migrate": "npm run prisma:migrate --prefix backend",
        "prisma:studio": "npm run prisma:studio --prefix backend",
        "backup": "npm run backup --prefix backend",
        "start": "concurrently \"npm start --prefix backend\" \"npm start --prefix frontend\""
    },
    "keywords": [
        "cv",
        "resume",
        "ai",
        "builder",
        "arabic",
        "french",
        "bilingual"
    ],
    "author": "CV Master AI Team",
    "license": "MIT",
    "devDependencies": {
        "concurrently": "^9.1.2"
    },
    "engines": {
        "node": ">=20.0.0",
        "npm": ">=9.0.0"
    },
    "dependencies": {
        "@supabase/supabase-js": "^2.86.0"
    }
}
