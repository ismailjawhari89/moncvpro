name = "moncvpro-worker"
main = "backend-worker/worker.js"
compatibility_date = "2025-12-02"

# Security Note:
# GROQ_API_KEY is stored in Cloudflare Secrets via `wrangler secret put GROQ_API_KEY`.
# Do NOT ADD secrets to this file!

[vars]
# Public variables only
ENVIRONMENT = "production"
LOG_LEVEL = "info"

# Observability (Optional)
# SENTRY_DSN = "..." 
